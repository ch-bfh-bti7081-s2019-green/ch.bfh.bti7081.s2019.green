image: registry.gitlab.com/severinkaderli/pandoc-docker:latest

stages:
  - Build

PDF:
  stage: Build
  script:
    - git clone https://gitlab.com/severinkaderli/pandoc-latex-templates .latex
    - .latex/build.sh ./diary.md
    - .latex/build.sh ./task_01/task_01.md
  artifacts:
    paths:
      - "*.pdf"
